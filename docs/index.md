---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stillbeat Provider"
description: |-
  Manage Stillbeat https://stillbeat.app cron/heartbeat checks as code.
---

# stillbeat Provider

Manage [Stillbeat](https://stillbeat.app) cron/heartbeat checks as code.

## Example Usage

```terraform
terraform {
  required_providers {
    stillbeat = {
      source = "antonefremov/stillbeat"
    }
  }
}

provider "stillbeat" {
  # endpoint defaults to the production API; override for staging/local:
  # endpoint = "https://staging.example"
  #
  # Prefer the STILLBEAT_API_KEY environment variable over setting api_key here,
  # to keep the key out of config and state.
  # api_key = var.stillbeat_api_key
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) Stillbeat API key (`dmf_...`), created in the dashboard under **API keys**. May also be set via `STILLBEAT_API_KEY` (preferred, keeps it out of state/config).
- `endpoint` (String) API base URL. Defaults to the production endpoint; override for staging/local. May also be set via `STILLBEAT_ENDPOINT`.
